---
import Header from './Header.astro';
import SubHeader from './SubHeader.astro';
import Image from './Image.astro';
import type { ComponentProps } from 'astro/types';
import { cn } from '../utils/tailwind';
interface Props {
  title: string
  subTitle?: string
  img?: ComponentProps<typeof Image>
}
const { title, subTitle, img } = Astro.props
---
<div
  class={cn(
    "p-ch-2 max-sm:p-ch bg-surface-0",
    { 'flex max-sm:flex-col gap-ch-2 max-sm:gap-0': img }
  )}>
  {img ?
  <Fragment>
    <Image src={img.src} alt={img.alt} class="min-w-56 h-56 max-sm:w-full object-cover" />
    <div class="flex-1">
      <Header variant="h3" class="mb-hch-05">{title}</Header>
      {subTitle && <SubHeader variant="h3">{subTitle}</SubHeader>}
      <div class="mt-hch text-subtext-1">
        <slot />
      </div>
    </div>
  </Fragment> :
  <Fragment>
    <Header variant="h3" class="mb-hch-05">{title}</Header>
    {subTitle && <SubHeader variant="h3">{subTitle}</SubHeader>}
    <div class="mt-hch text-base text-subtext-1">
      <slot />
    </div>
  </Fragment>
  }
</div>
